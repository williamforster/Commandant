{
  "name": "Commandant",
  "version": "1.0.0",
  "description": "Viewing geojson data on a map",
  "main": "main.js",
  "scripts": {
    "dev": "browserify -g [ babelify --presets [ \"@babel/preset-env\" ] ] --entry main.js > bundle.js",
    "build": "browserify -g [ babelify --presets [ \"@babel/preset-env\" ] ] -g uglifyify --entry main.js > bundle.js"
  },
  "dependencies": {
    "ol": "^5.1.3"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0-beta.56",
    "@babel/preset-env": "^7.0.0-beta.56",
    "babelify": "^9.0.0",
    "browserify": "^16.2.2",
    "uglifyify": "^5.0.1"
  }
}
